# KG 模块：线性结构

> **负责人**：成员 A
> **所属分类**：linear
> **知识点数量**：7 个

---

## 知识点列表

### 1. 线性表

| 属性 | 值 |
|------|-----|
| `name` | 线性表 |
| `description` | 具有相同数据类型的 n 个数据元素的有限序列 |
| `difficulty` | 1 |
| `category` | linear |

### 2. 顺序表

| 属性 | 值 |
|------|-----|
| `name` | 顺序表 |
| `description` | 用一组地址连续的存储单元依次存储线性表的数据元素 |
| `difficulty` | 2 |
| `category` | linear |

### 3. 链表

| 属性 | 值 |
|------|-----|
| `name` | 链表 |
| `description` | 通过指针链接的节点组成的线性表 |
| `difficulty` | 2 |
| `category` | linear |

### 4. 单链表

| 属性 | 值 |
|------|-----|
| `name` | 单链表 |
| `description` | 每个节点只有一个指向后继的指针 |
| `difficulty` | 2 |
| `category` | linear |

### 5. 双向链表

| 属性 | 值 |
|------|-----|
| `name` | 双向链表 |
| `description` | 节点同时有前驱指针和后继指针 |
| `difficulty` | 3 |
| `category` | linear |

### 6. 栈

| 属性 | 值 |
|------|-----|
| `name` | 栈 |
| `description` | 只能在表尾进行插入和删除的线性表（LIFO） |
| `difficulty` | 2 |
| `category` | linear |

### 7. 队列

| 属性 | 值 |
|------|-----|
| `name` | 队列 |
| `description` | 只能在表尾插入、表头删除的线性表（FIFO） |
| `difficulty` | 2 |
| `category` | linear |

---

## 关系列表

### SUBTOPIC_OF（层级包含）

| 子节点 | 父节点 | 说明 |
|-------|-------|------|
| 顺序表 | 线性表 | 顺序表是线性表的顺序存储实现 |
| 链表 | 线性表 | 链表是线性表的链式存储实现 |
| 单链表 | 链表 | 单链表是链表的基本形式 |
| 双向链表 | 链表 | 双向链表是链表的扩展形式 |

### PREREQUISITE_OF（前置依赖）

| 前置节点 | 后继节点 | 说明 |
|---------|---------|------|
| 线性表 | 栈 | 栈是操作受限的线性表 |
| 线性表 | 队列 | 队列是操作受限的线性表 |

---

## 与其他模块的关系

| 本模块节点 | 关系 | 其他模块节点 | 所属成员 |
|-----------|------|-------------|---------|
| 栈 | APPLIED_IN | DFS（图的遍历） | 成员 D |
| 队列 | APPLIED_IN | BFS（图的遍历） | 成员 D |
| 链表 | APPLIED_IN | 邻接表（图的存储） | 成员 D |
| 顺序表 | APPLIED_IN | 邻接矩阵（图的存储） | 成员 D |

> 注：APPLIED_IN 关系由成员 H 统一在跨关联文档中确认。
